"""Core Intelligence + AI Decision layers, organized as feature modules.

Each module owns one responsibility and exposes a clean service interface:
    ingestion, risk_memory, watchtower, blast_radius, business_impact,
    knowledge_graph, explain (AI Decision), reporting.
"""
