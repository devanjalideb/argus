# Normalize line endings: LF in the repository, native on checkout.
* text=auto eol=lf

# Windows-only scripts must retain CRLF to run correctly.
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary assets — never normalize.
*.pdf binary
*.png binary
*.jpg binary
*.ico binary
